# Handoff

No active worker handoff.

## Current State

The capability-core MarkItDown compact-stdout cleanup is complete and verified.

Code commit:

```text
89e6e26 fix: compact invoke output when writing files
```

## Resume Guidance

Resume from:

```text
C:/Users/User/AppData/Local/hermes/systems/capability-core/.hermes/active
```

Next work should be demand-driven:

```text
Use capability-core MarkItDown on the next real document, or add the next capability only when a real workflow needs it.
```

Do not add MCP server, dashboard, second capability, DK refactor, auto-ingest, or broad architecture changes unless the user explicitly selects that direction.
